# VibeCut Studio local environment variables
# Copy this file to `.env.local` if needed.

# Local transcription defaults
TRANSCRIPTION_PROVIDER=local
LOCAL_ALIGNER_URL=http://127.0.0.1:8765
WHISPERX_MODEL=large-v3
WHISPERX_DEVICE=cpu

# Gemini is still used for embeddings, search, and AI edit actions.
GEMINI_API_KEY=
